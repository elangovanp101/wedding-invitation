// Optional overrides for personalized invite links (/invite/[slug]). Any slug works even
// without an entry here (e.g. /invite/priya-family -> "Priya Family") — only add one if you
// want custom formatting, like "& Family" or a nickname.
export const guests: Record<string, string> = {
  priya: 'Priya & Family',
};
